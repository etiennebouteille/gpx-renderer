<!DOCTYPE html>
<html>
    <%- include('./partials/head.ejs', {title:"Latest Renders"}) %>
    <body>
        <%- include('./partials/header.ejs') %>
        <div id="latestContainer">
        <h2>Latest Renders</h2>
            <% if (renders.length > 0) { %>
                <% renders.forEach(renders => { %>
                    <% let str = "/renders/" %>
                    <img src=<%= str.concat(renders) %> >
                <% }) %>
            <% } %> 
        </div>
    </body>
</html>